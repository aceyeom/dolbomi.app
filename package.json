{
  "name": "dolbomi-app-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "DOLBOMI — 군인 자기개발 앱. A Vite + React SPA on Supabase, live at dolbomi.app/.",
  "scripts": {
    "dev": "npm --prefix dolbomi-app run dev",
    "build": "npm --prefix dolbomi-app install && npm --prefix dolbomi-app run build -- --outDir ../dist --emptyOutDir",
    "gen:seed": "npm --prefix dolbomi-app run gen:seed"
  }
}
